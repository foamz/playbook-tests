---
- name: install nginx
  hosts: all
  tasks:
  - name: yum install nginx
    yum:
      name: nginx
      state: present
      
 - name: start nginx
   hosts: all
   service: 
      name: nginx
      enabled: yes
      state: started
